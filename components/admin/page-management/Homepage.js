import { Typography } from '@mui/material';

export default function HomePage() {
	return (
		<div>
			<Typography variant='h2'>Why Zes</Typography>

			<Typography variant='h2'>Services</Typography>

			<Typography variant='h2'>Testimonials</Typography>
		</div>
	);
}
